# www → apex redirects are not supported here (Workers _redirects requires relative paths).
# Configure in Cloudflare Dashboard → Rules → Redirect Rules:
#   If hostname equals www.doxastudios.ca → redirect to https://doxastudios.ca/$1 (301)
